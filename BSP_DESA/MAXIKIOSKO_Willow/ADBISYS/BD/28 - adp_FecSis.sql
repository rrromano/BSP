Use WIADBISYS
Go 
If Exists ( Select 1 From SysObjects Where Name = 'adp_FecSis')
  Drop Procedure adp_FecSis
Go 

-- SP PARA OBTENER/ACTUALIZAR LA FECHA DE SISTEMA, CORRESPONDIENTE A LA CAJA.
Create procedure dbo.adp_FecSis (@FECHA DATETIME = NULL)
as


BEGIN TRY

	SET NOCOUNT ON
	
	IF (@FECHA IS NULL)
	BEGIN
	
		SELECT convert(varchar,Fecha_Sistema,120) AS FECHA_SISTEMA FROM PARAMETROS_GENERALES
		
	END ELSE BEGIN
	
		SELECT 'FECHA DE SISTEMA ACTUALIZADA CORRECTAMENTE'
		SELECT  'FECHA ANTERIOR ' + convert(varchar,Fecha_Sistema,120) AS FECHA_SISTEMA FROM PARAMETROS_GENERALES
		
		UPDATE A
		SET A.Fecha_Sistema = @FECHA
		FROM PARAMETROS_GENERALES A
		
		SELECT  'FECHA ACTUAL ' + convert(varchar,Fecha_Sistema,120) AS FECHA_SISTEMA FROM PARAMETROS_GENERALES
	END
	

	SET NOCOUNT OFF
	
END TRY

BEGIN CATCH
  SET NOCOUNT OFF
  PRINT 'ACTUALIZACION CANCELADA POR ERROR'
  SELECT ERROR_NUMBER()     'ERROR_NUMBER' , 
         ERROR_MESSAGE()    'ERROR_MESSAGE', 
         ERROR_LINE()       'ERROR_LINE', 
         ERROR_PROCEDURE()  'ERROR_PROCEDURE', 
         ERROR_SEVERITY ()  'ERROR_SEVERITY',   
         ERROR_STATE()      'ERROR_STATE'
END CATCH	

go
